<Window x:Class="EKSE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EKSE"
        xmlns:components="clr-namespace:EKSE.Components"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="万物键盘音效" 
        Height="600" 
        Width="1000"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Opacity="1"
        Icon="Assets/Icons/logo.ico">
    <Window.Resources>
        <!-- 淡入淡出动画 -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainContentArea"
                             Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.0"
                             Duration="0:0:0.15"/>
        </Storyboard>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainContentArea"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0"
                             Duration="0:0:0.15"/>
        </Storyboard>
        
        <!-- 淡入动画 -->
        <Storyboard x:Key="SlideInFromLeftStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainContentArea"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0"
                             Duration="0:0:0.3"/>
        </Storyboard>
        
        <!-- 启动淡入动画 -->
        <Storyboard x:Key="VerticalSlideStartupStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0"
                             Duration="0:0:0.8"/>
        </Storyboard>
        
        <!-- 最小化淡出动画 -->
        <Storyboard x:Key="MinimizeSlideOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.0"
                             Duration="0:0:0.5"/>
        </Storyboard>
        
        <!-- 关闭淡出动画 -->
        <Storyboard x:Key="CloseSlideOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.0"
                             Duration="0:0:0.5"/>
        </Storyboard>
        

        
        <!-- 缓动函数 -->
        <CircleEase x:Key="CircleEase" EasingMode="EaseOut"/>
    </Window.Resources>
    
    <materialDesign:DialogHost>
        <Border Background="{DynamicResource MaterialDesignPaper}" 
                CornerRadius="10"
                BorderBrush="{DynamicResource MaterialDesignDivider}" 
                BorderThickness="1"
                Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 标题栏 -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource TitleBarBackground}"
                        CornerRadius="10,10,0,0"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 应用标题 -->
                        <TextBlock Grid.Column="0" 
                                   Text="万物键盘音效" 
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="15,0,0,0"
                                   FontSize="16"
                                   FontWeight="Medium"/>
                        
                        <!-- 窗口控制按钮 -->
                        <StackPanel Grid.Column="1" 
                                    Orientation="Horizontal"
                                    Margin="0,0,10,0">
                            <Button x:Name="MinimizeButton"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="White"
                                    Click="MinimizeButton_Click"
                                    ToolTip="最小化">
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.8" To="1.0" Duration="0:0:0.2"/>
                                                <ColorAnimation Storyboard.TargetProperty="Foreground.Color" From="White" To="#FFD700" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1.0" To="0.8" Duration="0:0:0.2"/>
                                                <ColorAnimation Storyboard.TargetProperty="Foreground.Color" From="#FFD700" To="White" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Button.Triggers>
                                <materialDesign:PackIcon Kind="WindowMinimize"/>
                            </Button>
                            
                            <Button x:Name="MaximizeButton"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="White"
                                    Click="MaximizeButton_Click"
                                    ToolTip="最大化">
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.8" To="1.0" Duration="0:0:0.2"/>
                                                <ColorAnimation Storyboard.TargetProperty="Foreground.Color" From="White" To="#FFD700" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1.0" To="0.8" Duration="0:0:0.2"/>
                                                <ColorAnimation Storyboard.TargetProperty="Foreground.Color" From="#FFD700" To="White" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Button.Triggers>
                                <materialDesign:PackIcon Name="MaximizeIcon" Kind="WindowMaximize"/>
                            </Button>
                            
                            <Button x:Name="CloseButton"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="White"
                                    Click="CloseButton_Click"
                                    ToolTip="关闭">
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.8" To="1.0" Duration="0:0:0.2"/>
                                                <ColorAnimation Storyboard.TargetProperty="Foreground.Color" From="White" To="#FF4500" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1.0" To="0.8" Duration="0:0:0.2"/>
                                                <ColorAnimation Storyboard.TargetProperty="Foreground.Color" From="#FF4500" To="White" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Button.Triggers>
                                <materialDesign:PackIcon Kind="WindowClose"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- 主内容区域 -->
                <Border Grid.Row="1" 
                        Background="{DynamicResource MaterialDesignPaper}"
                        CornerRadius="0,0,10,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                        <!-- 侧边栏 -->
                        <Border Grid.Column="0" 
                                Background="{DynamicResource MaterialDesignBackground}"
                                CornerRadius="0,0,0,10">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="SidebarPanel" 
                                            Margin="0,20"/>
                            </ScrollViewer>
                        </Border>

                        <!-- 主内容区 -->
                        <Border Grid.Column="1" 
                                CornerRadius="0,0,10,0">
                            <Grid>
                                <ContentPresenter x:Name="MainContentArea" Opacity="1"/>
                                
                                <!-- 托盘图标 -->
                                <tb:TaskbarIcon x:Name="MyNotifyIcon"
                                                Visibility="Visible"
                                                ToolTipText="万物键盘音效"
                                                IconSource="Assets/Icons/logo.ico"
                                                NoLeftClickDelay="True"
                                                TrayLeftMouseUp="ShowWindowMenuItem_Click">
                                    <tb:TaskbarIcon.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="显示窗口" Click="ShowWindowMenuItem_Click" />
                                            <Separator />
                                            <MenuItem Header="退出" Click="ExitApplicationMenuItem_Click" />
                                        </ContextMenu>
                                    </tb:TaskbarIcon.ContextMenu>
                                </tb:TaskbarIcon>
                            </Grid>
                        </Border>
                        

                    </Grid>
                </Border>
            </Grid>
        </Border>
    </materialDesign:DialogHost>
</Window>