<Window x:Class="EKSE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EKSE"
        xmlns:components="clr-namespace:EKSE.Components"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="万物键盘音效" 
        Height="600" 
        Width="1000"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Opacity="1"
        Icon="Assets/Icons/logo.ico">
    <Window.Resources>
        <!-- 淡入淡出动画 -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainContentArea"
                             Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.0"
                             Duration="0:0:0.15"/>
        </Storyboard>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainContentArea"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0"
                             Duration="0:0:0.15"/>
        </Storyboard>
        
        <!-- 滑动动画 -->
        <Storyboard x:Key="SlideInFromLeftStoryboard">
            <ThicknessAnimation Storyboard.TargetName="MainContentArea"
                                Storyboard.TargetProperty="Margin"
                                From="-50,0,0,0" To="0,0,0,0"
                                Duration="0:0:0.3">
                <ThicknessAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetName="MainContentArea"
                             Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0"
                             Duration="0:0:0.3"/>
        </Storyboard>
        
        <!-- 垂直滑动启动动画 -->
        <Storyboard x:Key="VerticalSlideStartupStoryboard">
            <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                From="0,-600,0,0" To="0,0,0,0"
                                Duration="0:0:0.8">
                <ThicknessAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0.0" To="1.0"
                             Duration="0:0:0.8"/>
        </Storyboard>
        
        <!-- 最小化滑出动画 -->
        <Storyboard x:Key="MinimizeSlideOutStoryboard">
            <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                From="0,0,0,0" To="0,600,0,0"
                                Duration="0:0:0.5">
                <ThicknessAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.0"
                             Duration="0:0:0.5"/>
        </Storyboard>
        
        <!-- 关闭滑出动画 -->
        <Storyboard x:Key="CloseSlideOutStoryboard">
            <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                From="0,0,0,0" To="0,-600,0,0"
                                Duration="0:0:0.5">
                <ThicknessAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.0"
                             Duration="0:0:0.5"/>
        </Storyboard>
        
        <!-- 涟漪效果动画 -->
        <Storyboard x:Key="RippleEffectStoryboard">
            <DoubleAnimation Storyboard.TargetName="RippleEllipse"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.0" To="1.0"
                             Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="RippleEllipse"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.0" To="1.0"
                             Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CircleEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="RippleEllipse"
                             Storyboard.TargetProperty="Opacity"
                             From="0.5" To="0"
                             Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- 缓动函数 -->
        <CircleEase x:Key="CircleEase" EasingMode="EaseOut"/>
    </Window.Resources>
    
    <materialDesign:DialogHost>
        <Border Background="{DynamicResource MaterialDesignPaper}" 
                CornerRadius="10"
                BorderBrush="{DynamicResource MaterialDesignDivider}" 
                BorderThickness="1"
                Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" 
                                  ShadowDepth="3" 
                                  BlurRadius="10" 
                                  Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 标题栏 -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource TitleBarBackground}"
                        CornerRadius="10,10,0,0"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 应用标题 -->
                        <TextBlock Grid.Column="0" 
                                   Text="万物键盘音效" 
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="15,0,0,0"
                                   FontSize="16"
                                   FontWeight="Medium"/>
                        
                        <!-- 窗口控制按钮 -->
                        <StackPanel Grid.Column="1" 
                                    Orientation="Horizontal"
                                    Margin="0,0,10,0">
                            <Button x:Name="MinimizeButton"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="White"
                                    Click="MinimizeButton_Click"
                                    ToolTip="最小化">
                                <Button.Effect>
                                    <DropShadowEffect x:Name="MinimizeShadow" 
                                                      ShadowDepth="0" 
                                                      BlurRadius="10" 
                                                      Color="Yellow"
                                                      Opacity="0"/>
                                </Button.Effect>
                                <Button.Resources>
                                    <Storyboard x:Key="MinimizeButtonHoverOn">
                                        <DoubleAnimation Storyboard.TargetName="MinimizeShadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1.0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="MinimizeShadow"
                                                         Storyboard.TargetProperty="BlurRadius"
                                                         From="10" To="25" Duration="0:0:0.1"/>
                                    </Storyboard>
                                    <Storyboard x:Key="MinimizeButtonHoverOff">
                                        <DoubleAnimation Storyboard.TargetName="MinimizeShadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1.0" To="0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="MinimizeShadow"
                                                         Storyboard.TargetProperty="BlurRadius"
                                                         From="25" To="10" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </Button.Resources>
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                        <BeginStoryboard Storyboard="{StaticResource MinimizeButtonHoverOn}"/>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                        <BeginStoryboard Storyboard="{StaticResource MinimizeButtonHoverOff}"/>
                                    </EventTrigger>
                                </Button.Triggers>
                                <materialDesign:PackIcon Kind="WindowMinimize"/>
                            </Button>
                            
                            <Button x:Name="MaximizeButton"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="White"
                                    Click="MaximizeButton_Click"
                                    ToolTip="最大化">
                                <Button.Effect>
                                    <DropShadowEffect x:Name="MaximizeShadow" 
                                                      ShadowDepth="0" 
                                                      BlurRadius="10" 
                                                      Color="Yellow"
                                                      Opacity="0"/>
                                </Button.Effect>
                                <Button.Resources>
                                    <Storyboard x:Key="MaximizeButtonHoverOn">
                                        <DoubleAnimation Storyboard.TargetName="MaximizeShadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1.0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="MaximizeShadow"
                                                         Storyboard.TargetProperty="BlurRadius"
                                                         From="10" To="25" Duration="0:0:0.1"/>
                                    </Storyboard>
                                    <Storyboard x:Key="MaximizeButtonHoverOff">
                                        <DoubleAnimation Storyboard.TargetName="MaximizeShadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1.0" To="0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="MaximizeShadow"
                                                         Storyboard.TargetProperty="BlurRadius"
                                                         From="25" To="10" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </Button.Resources>
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                        <BeginStoryboard Storyboard="{StaticResource MaximizeButtonHoverOn}"/>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                        <BeginStoryboard Storyboard="{StaticResource MaximizeButtonHoverOff}"/>
                                    </EventTrigger>
                                </Button.Triggers>
                                <materialDesign:PackIcon Name="MaximizeIcon" Kind="WindowMaximize"/>
                            </Button>
                            
                            <Button x:Name="CloseButton"
                                    Style="{StaticResource MaterialDesignFlatButton}"
                                    Foreground="White"
                                    Click="CloseButton_Click"
                                    ToolTip="关闭">
                                <Button.Effect>
                                    <DropShadowEffect x:Name="CloseShadow" 
                                                      ShadowDepth="0" 
                                                      BlurRadius="10" 
                                                      Color="Yellow"
                                                      Opacity="0"/>
                                </Button.Effect>
                                <Button.Resources>
                                    <Storyboard x:Key="CloseButtonHoverOn">
                                        <DoubleAnimation Storyboard.TargetName="CloseShadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1.0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="CloseShadow"
                                                         Storyboard.TargetProperty="BlurRadius"
                                                         From="10" To="25" Duration="0:0:0.1"/>
                                    </Storyboard>
                                    <Storyboard x:Key="CloseButtonHoverOff">
                                        <DoubleAnimation Storyboard.TargetName="CloseShadow"
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1.0" To="0" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="CloseShadow"
                                                         Storyboard.TargetProperty="BlurRadius"
                                                         From="25" To="10" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </Button.Resources>
                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                        <BeginStoryboard Storyboard="{StaticResource CloseButtonHoverOn}"/>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                        <BeginStoryboard Storyboard="{StaticResource CloseButtonHoverOff}"/>
                                    </EventTrigger>
                                </Button.Triggers>
                                <materialDesign:PackIcon Kind="WindowClose"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- 主内容区域 -->
                <Border Grid.Row="1" 
                        Background="{DynamicResource MaterialDesignPaper}"
                        CornerRadius="0,0,10,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 侧边栏 -->
                        <Border Grid.Column="0" 
                                Background="{DynamicResource MaterialDesignBackground}"
                                CornerRadius="0,0,0,10">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="SidebarPanel" 
                                            Margin="0,20"/>
                            </ScrollViewer>
                        </Border>

                        <!-- 主内容区 -->
                        <Border Grid.Column="1" 
                                CornerRadius="0,0,10,0">
                            <Grid>
                                <ContentPresenter x:Name="MainContentArea" Opacity="1"/>
                                
                                <!-- 托盘图标 -->
                                <tb:TaskbarIcon x:Name="MyNotifyIcon"
                                                Visibility="Visible"
                                                ToolTipText="万物键盘音效"
                                                IconSource="Assets/Icons/logo.ico"
                                                NoLeftClickDelay="True"
                                                TrayLeftMouseUp="ShowWindowMenuItem_Click">
                                    <tb:TaskbarIcon.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="显示窗口" Click="ShowWindowMenuItem_Click" />
                                            <Separator />
                                            <MenuItem Header="退出" Click="ExitApplicationMenuItem_Click" />
                                        </ContextMenu>
                                    </tb:TaskbarIcon.ContextMenu>
                                </tb:TaskbarIcon>
                            </Grid>
                        </Border>
                        
                        <!-- 涟漪效果元素 -->
                        <Ellipse x:Name="RippleEllipse"
                                 Grid.ColumnSpan="2"
                                 Fill="{DynamicResource TitleBarBackground}"
                                 Opacity="0"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center">
                            <Ellipse.RenderTransform>
                                <ScaleTransform x:Name="RippleScaleTransform" ScaleX="0" ScaleY="0"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </materialDesign:DialogHost>
</Window>