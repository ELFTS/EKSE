<UserControl x:Class="EKSE.Views.SponsorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                   Text="赞助我们"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- 赞助说明文字 -->
        <TextBlock Grid.Row="1"
                   Style="{StaticResource MaterialDesignBody1TextBlock}"
                   TextWrapping="Wrap"
                   LineHeight="24"
                   Margin="0,0,0,30"
                   Text="我们是游牧民族（无业游民），如果你喜欢我们开发的软件，欢迎捐赠我们让我们吃上香喷喷的美食吧～"/>

        <!-- 收款码区域 -->
        <Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧收款码 -->
            <StackPanel Grid.Column="0" 
                       Margin="0,0,20,0" 
                       HorizontalAlignment="Right">
                <TextBlock Text="微信收款码" 
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                
                <Image x:Name="WeChatQRCode"
                       Width="300"
                       Height="300"
                       Stretch="Uniform"/>
            </StackPanel>
            
            <!-- 右侧收款码 -->
            <StackPanel Grid.Column="1" 
                       Margin="20,0,0,0" 
                       HorizontalAlignment="Left">
                <TextBlock Text="支付宝收款码" 
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                
                <Image x:Name="AlipayQRCode"
                       Width="300"
                       Height="300"
                       Stretch="Uniform"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>